# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Simple Python 3.12+ project with a minimal structure and pydantic dependency.

## Development Commands

**Setup environment:**
```bash
pip install -r requirements.txt
```

**Run the application:**
```bash
python main.py
```

## Architecture

Single-file application with no complex architecture. The main entry point is `main()` in main.py:1.
